import { ChatOnboarding } from '@/components/onboarding/ChatOnboarding';

export const metadata = {
  title: 'Get started — Sembli',
};

export default function OnboardingPage() {
  return <ChatOnboarding />;
}
